name: "DISABLED: Weekly Checks (Daily)"
on:
  push:
    branches:
      - __disabled__

# This workflow is intentionally disabled and left as a stub to prevent accidental triggers.
# The active automation now uses industry-research.yml (Gemini) for daily reports.

jobs: {}


{
  "name": "fpf-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'lint not configured (consider biome or eslint)' && exit 0",
    "fmt": "echo 'format not configured (consider biome or prettier)' && exit 0",
    "mcp:fpf": "bun run scripts/mcp/server.ts",
    "mcp:fpf:sse": "PORT=${PORT:-3333} bun run scripts/mcp/server-sse.ts",
    "yadisk:sync": "bun run scripts/yadisk-sync.mjs",
    "meme:meter": "bun run scripts/agentic/meme-meter.ts",
    "gpt:review": "bun run scripts/gpt-code-review.ts",
    "gpt:task": "bun run scripts/gpt-task-runner.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.6.3"
  }
}

{
  "name": "fpf-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.9",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "sync": "bun run src/sync-fpf.ts",
    "check": "bun run lint && bun run typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "ky": "^1.14.3"
  }
}

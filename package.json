{
  "name": "fpf-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.9",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "sync": "bun run src/sync-fpf.ts",
    "sync:dry-run": "bun run src/sync-fpf.ts --dry-run",
    "check": "bun run lint && bun run typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@types/node": "^25.3.3",
    "typescript": "^5.9.3"
  }
}

{
  "name": "fpf-pageindex-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.9",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "memory": "bun run src/memory-cli.ts",
    "check": "bun run lint",
    "lint": "bun run typecheck",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "effect": "^3.21.0"
  }
}

{
  "name": "fpf-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "oxlint .",
    "fmt": "echo 'format: use oxlint for linting, add prettier if formatting needed' && exit 0",
    "yadisk:sync": "bun run scripts/yadisk-sync.mjs",
    "agentic:diff-eval": "bash scripts/agentic/diff-eval.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "^1.3.0",
    "oxlint": "^1.26.0",
    "typescript": "^5.6.3"
  }
}
